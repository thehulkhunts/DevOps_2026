apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: external-secrets 
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<AWS-ACCOUNT-ID>:role/eks-prod-eso-role

    #This service account will attached to above xreated role.
    # kubectl apply -f eso-serviceaccount.yaml