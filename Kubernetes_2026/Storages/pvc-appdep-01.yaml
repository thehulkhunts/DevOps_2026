apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: web-app-pvc-claim
  namespace: dev 
spec:
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: storageclass-01
